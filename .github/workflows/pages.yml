name: Deploy to GitHub Pages

on:
  push:
    branches: [main, master]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 20
          cache: npm
      - name: Configure GitHub Pages URL
        run: |
          repo_name="${GITHUB_REPOSITORY#*/}"
          owner_name="${GITHUB_REPOSITORY_OWNER,,}"
          if [ "${repo_name,,}" = "${GITHUB_REPOSITORY_OWNER,,}.github.io" ]; then
            echo "NEXT_PUBLIC_BASE_PATH=" >> "$GITHUB_ENV"
            echo "NEXT_PUBLIC_SITE_URL=https://${owner_name}.github.io" >> "$GITHUB_ENV"
            echo "NEXT_PUBLIC_URL=https://${owner_name}.github.io" >> "$GITHUB_ENV"
          else
            echo "NEXT_PUBLIC_BASE_PATH=/${repo_name}" >> "$GITHUB_ENV"
            echo "NEXT_PUBLIC_SITE_URL=https://${owner_name}.github.io/${repo_name}" >> "$GITHUB_ENV"
            echo "NEXT_PUBLIC_URL=https://${owner_name}.github.io/${repo_name}" >> "$GITHUB_ENV"
          fi
      - run: npm ci
      - name: Build static site
        run: |
          node scripts/write-atproto-client-metadata.mjs
          node node_modules/next/dist/bin/next build
          node node_modules/next/dist/bin/next export
      - uses: actions/upload-pages-artifact@v3
        with:
          path: out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
